# Design

화성특례시 공식 행정 도구로 읽히는 격자 분석 콘솔. 관공서가 발행한 문서의 규율
— 인장, 괘선, 조(條) 단위 구획 — 을 화면으로 옮긴 것이 이 세계의 뼈대입니다.

대시보드의 카테고리 기본값(어두운 판에 네온 차트 타일을 흩는 배치)은 쓰지 않습니다.
이 화면을 쓰는 사람은 낮에 사무실 데스크톱 앞에 앉아 있고, 그가 만든 결과물은
회의 자료로 인쇄돼 나갑니다. 그래서 종이에 가까운 밝은 바탕이 기본이고, 카드는
떠 있지 않고 선으로 나뉩니다.

## 색

공식 CI(`assets/img/hwaseong-ci.png`, BI 원본에서 추출)에서 그대로 가져왔습니다.

| 토큰 | 값 | 쓰는 곳 |
|---|---|---|
| `--brand` | `#0054A6` | 상단 레일, 활성 내비·탭, 카드 제목 표식, 주요 버튼, 포커스 링 |
| `--brand-2` | `#F36F21` | **활성 탭 밑줄 하나뿐** |
| `--brand-2-ink` | `#C2410C` | 주황 계열로 글자를 써야 할 때 |
| `--bg` / `--card` | `#EEF2F7` / `#FFFFFF` | 바탕 / 카드 |
| `--ink` / `--ink2` / `--ink3` | `#12233A` / `#41546E` / `#5D6E85` | 본문 / 보조 / 3차 |

**주황을 글자에 쓰지 않는 이유** — 공식 `#F36F21` 은 흰 바탕에서 대비 2.95:1 로
WCAG AA(4.5:1)는 물론 큰 글자 기준(3:1)도 넘지 못합니다. 채움과 마커로만 쓰고,
글자가 필요하면 `--brand-2-ink`(5.2:1)를 씁니다.

**크롬과 캔버스를 가릅니다.** 브랜드 파랑·주황은 상단 레일·내비·버튼 같은 크롬에만
있습니다. 지도와 차트의 색(`--mi*` `--sb*` `--so*` `--fl*` `--c-board` `--c-alight`)은 값을
나르는 지표라 따로 두며 브랜드 색이 침범하지 않습니다. 둘은 화면에서 위치로 갈립니다.

다크 테마는 같은 계열을 밝게 올립니다(`--brand:#5AA6EA`). 공식 CI 는 어두운 배경에서
2.25:1 이라 못 읽으므로, 다크에서만 로고 뒤에 흰 판을 깝니다 — 로고 자체를 고치는 것은
CI 변형이라 하지 않습니다.

**지표 스케일은 층마다 색상이 달라야 합니다.** 수요 `--sb`(파랑) · 공급 `--so`(주황) ·
유동인구 `--fl`(자주). 예전에는 수요와 유동인구가 `--sb` 를 같이 써서, 층을 번갈아
켜도 지도가 똑같아 무엇을 보고 있는지 알 수 없었습니다.

**심각할수록 진하게.** 라이트에서는 단계가 오를수록 어두워집니다(L\* 93 → 36).
다크에서는 어둡게 갈 수 없습니다 — 바탕(`--land:#232a33`)이 이미 L\* 16 이라 그보다
어두우면 가장 중요한 칸이 배경에 잠깁니다. 그래서 **채도로 세기를 나릅니다**(8 → 52~64,
L\* 는 22 → 52 로 완만히). 예전에는 반대로 심각한 쪽이 파스텔로 빠져(`--mi6` 이
L\* 70 · 채도 40 인 살구색) 옅을수록 심하다는 뜻이 됐습니다.
단색 막대(`.cost-bar`, 예산 막대)는 중간 단계가 아니라 **끝 단계**(`--sb4`)를 씁니다.

## 타이포

Pretendard(CDN, 실패 시 시스템 스택으로 폴백). Windows 기본 맑은 고딕은 자간과 획이
낡아 같은 레이아웃도 오래된 화면처럼 보입니다.

- `h1` 23px / 800 / `-.03em` · 카드 제목 15px / 700 / `-.02em`
- 본문 14px, 보조 11.5~13px
- `font-variant-numeric: tabular-nums` 를 body 에 겁니다 — 지표가 갱신될 때
  자릿수가 흔들리지 않아야 합니다.
- 산문 요소에는 `word-break: keep-all` (한글이 음절 단위로 쪼개지지 않게).

## 구조

- **상단 레일** — 흰 바탕에 공식 CI + 서비스명, 아래 `--brand` 3px 선. 이 선이
  화면 전체를 "시가 발행한 문서"로 묶습니다. CI 옆 서비스명은 짧게(`APP.navName`)
  둡니다 — CI 가 이미 기관을 말하므로 '화성'을 되풀이하지 않습니다.
- **카드** — 1px 괘선 + 12px 라운드. 그림자로 띄우지 않습니다. 그림자는 실제로 떠야
  하는 것(모달·툴팁)에만 남겨 그 뜻을 지킵니다.
- **카드 제목** — 왼쪽에 `--brand` 3×14px 표식을 세워 조의 머리를 엽니다. 아래 1px
  괘선으로 제목과 내용을 가릅니다.
- **KPI** — 넷을 각각 띄우지 않고 **한 장의 띠**로 묶고 사이만 괘선으로 가릅니다.
  화면의 상자 수가 넷 줄어 지도가 주인공 자리를 지킵니다. 2열로 접히면 세로 괘선을
  홀수 칸에서 걷고 아랫줄에 가로 괘선을 답니다.
- **요약 상자(.brief)** — 브랜드 계열 옅은 바탕. 색깔 사이드탭(`border-left:3px`)은
  쓰지 않습니다 — AI 가 만든 화면의 대표 표식이라 검출기도 잡습니다.

## 아이콘

`core.js` 의 `HW.icon(name)` 한 벌만 씁니다. 24 격자 · 획 1.7 · 둥근 끝 · 채움 없음 ·
`currentColor`. 유니코드 글자(`▤` `✦`)를 아이콘 자리에 쓰지 않습니다 — 폰트마다 모양과
굵기가 달라 나란히 놓으면 한 벌로 보이지 않고, 일부 폰트에서는 네모로 뜹니다.

## 브라우저 기본 표면

선택 영역·캐럿·스크롤바·포커스 링을 브랜드 색으로 맞춥니다. 손대지 않으면 어느
디자인에도 속하지 않는 OS 기본색으로 남고, 그것이 "조립된 화면"의 가장 흔한 신호입니다.

## 움직임

배치를 바꿀 때 예산 막대가 0.3초로 차오르는 것 하나만 authored motion 으로 둡니다.
폭이 아니라 `transform: scaleX()` 로 그려 레이아웃을 다시 계산하지 않습니다.
`prefers-reduced-motion` 에서는 끕니다.

## 하지 않는 것

- 제목 위 eyebrow/kicker — 제목이 스스로 못 버틴다는 신호입니다.
- 색깔 사이드탭, 그라디언트 글자, 장식용 glass/blur.
- 이모지·유니코드 글자를 아이콘 자리에.
- 지도·차트 색을 장식으로 재사용하기.

## 검증 방법

```bash
node tools/check-css.js      # 주석 짝·중괄호 짝·핵심 레이아웃 규칙 8개
node tools/e2e-live.js       # 실서버 왕복 33건
node <impeccable>/scripts/detect.mjs --json <파일들>   # 기계적 안티패턴
```

명암비는 눈대중하지 않고 계산해서 확인합니다(위 색 표의 수치가 그 결과입니다).
