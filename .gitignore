# 주피터
.ipynb_checkpoints/

# 배포용 압축본
*.zip

# 의존성 (현재 프로젝트는 외부 라이브러리를 쓰지 않습니다)
node_modules/

# 에디터·도구
.omc/
.vscode/

# OS
.DS_Store
Thumbs.db

# SGIS 원본 경계 SHP (135MB). 여기서 assets/data/boundary.js 를 만들어 쓰므로
# 저장소에는 넣지 않습니다. 다시 만들려면 SGIS 에서 내려받아
#   python3 tools/build-boundary.py
bnd_dong_*.shp
bnd_dong_*.dbf
bnd_dong_*.shx
bnd_dong_*.prj
bnd_dong_*.cpg
