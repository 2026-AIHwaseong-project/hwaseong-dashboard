/* ============================================================================
 *  config.js  —  서버 주소 등 배포·시연 설정은 여기만 고치면 됩니다.
 * ----------------------------------------------------------------------------
 *  프론트엔드의 모든 서버 통신은 assets/js/api.js 한 곳을 지나가며,
 *  전부 백엔드(FastAPI) 실서버로 갑니다. 엔드포인트 목록은 백엔드 docs/API_SPEC.md 참고.
 * ========================================================================= */
(function (global) {
  'use strict';

  /* ====================================================================
   * ★ 백엔드 서버 주소 — 배포·시연 때 여기 한 줄만 바꾸면 됩니다.
   *
   *   SERVER_URL = ''                              → 자동 결정 (아래 규칙)
   *   SERVER_URL = 'https://xxxx.ngrok-free.app'   → ngrok 등 터널 주소
   *   SERVER_URL = 'http://192.168.0.10:8000'      → 다른 PC 의 백엔드
   *
   *   자동 결정 규칙(빈 문자열일 때):
   *     - file:// 로 열었을 때              → http://localhost:8000
   *     - 백엔드가 직접 서빙(/app)했을 때    → 같은 원점 (포트 그대로)
   *     - 개발 서버(예: :5500)로 서빙했을 때 → 같은 호스트의 :8000
   *
   *   ⚠️ 터널 주소를 여기에 적어 커밋하지 마세요.
   *      ngrok 무료 터널은 재시작마다 주소가 바뀝니다. 커밋해 두면 그 순간
   *      저장소를 클론한 모두가 죽은 주소를 보게 되고, 공개 저장소라면
   *      살아 있는 동안 누구나 그 PC 로 접근할 수 있습니다.
   *
   *   시연 때는 코드를 고치지 말고 주소 뒤에 ?server= 를 붙이세요.
   *      index.html?server=https://xxxx.ngrok-free.app
   *   한 번 열면 브라우저에 기억되고, ?server= 만 붙이면 지워집니다.
   * ================================================================== */
  var SERVER_URL = 'http://localhost:8000';

  /* URL 쿼리(?server=)로 덮어쓰기. 코드를 안 고치고 시연 대상을 바꾸기 위한 것.
     값이 있으면 localStorage 에 기억하고, 빈 값(?server=)이면 지웁니다.
     우선순위: ?server=(저장된 주소) → 위의 SERVER_URL → 자동 결정.
     사용자가 명시한 주소가 하드코딩보다 우선입니다 — 예전엔 순서가 반대라
     SERVER_URL 이 채워져 있으면 ?server= 가 조용히 무시됐습니다. */
  try {
    var _q = new URLSearchParams(location.search).get('server');
    if (_q !== null) {
      if (_q) { localStorage.setItem('hw.serverUrl', _q); }
      else { localStorage.removeItem('hw.serverUrl'); }
    }
    SERVER_URL = localStorage.getItem('hw.serverUrl') || SERVER_URL || '';
  } catch (e) { /* file:// 등에서 localStorage 가 막히면 자동 결정으로 간다 */ }

  var CONFIG = {

    /* ------------------------------------------------------------------
     * [1] 서버 주소
     *   위의 SERVER_URL 이 우선합니다. 비워 두면 자동 결정.
     * ---------------------------------------------------------------- */
    BASE_URL: SERVER_URL || (function () {
      try {
        if (location.protocol === 'file:') return 'http://localhost:8000';
        /* 8000 이거나 포트 표기가 없으면(80/443 — 터널·배포) 백엔드가 직접
           서빙 중인 것으로 보고 같은 원점을 씁니다. 그 외(개발 서버)는 :8000. */
        if (location.port === '8000' || !location.port) return '';
        return location.protocol + '//' + location.hostname + ':8000';
      } catch (e) { return 'http://localhost:8000'; }
    })(),

    /* ------------------------------------------------------------------
     * [1-2] 추가 요청 헤더
     *   ngrok 무료 터널은 브라우저 요청 앞에 경고 페이지(HTML)를 끼워 넣어
     *   JSON 파싱이 깨집니다. 이 헤더가 있으면 경고 없이 바로 통과합니다.
     * ---------------------------------------------------------------- */
    EXTRA_HEADERS: /ngrok/.test(SERVER_URL)
      ? { 'ngrok-skip-browser-warning': 'true' }
      : {},

    /* API 경로 접두사. 백엔드 라우팅에 맞춰 바꾸세요. */
    API_PREFIX: '/api/v1',

    /* ------------------------------------------------------------------
     * [2] 인증
     *   백엔드가 토큰을 요구하면 여기에 설정합니다.
     *   getToken 은 함수여도 됩니다(로그인 후 동적으로 읽을 때).
     * ---------------------------------------------------------------- */
    AUTH: {
      enabled: false,
      header: 'Authorization',
      scheme: 'Bearer',
      getToken: function () {
        // 예) return localStorage.getItem('accessToken');
        return '';
      }
    },

    /* 요청 타임아웃(ms). AI 보고서 생성은 오래 걸리므로 따로 둡니다. */
    TIMEOUT_MS: 15000,
    TIMEOUT_MS_REPORT: 120000,

    /* ------------------------------------------------------------------
     * [3] 보고서 내보내기 방식
     *   'client' → 브라우저에서 직접 파일을 만듭니다. (서버 없이 동작, 기본값)
     *   'server' → POST {API_PREFIX}/reports/export 로 파일을 받아옵니다.
     *              한글 .hwpx 원본 포맷이 필요하면 서버 방식을 쓰세요.
     *   'auto'   → 서버를 먼저 시도하고, 실패하면 클라이언트로 폴백합니다.
     * ---------------------------------------------------------------- */
    EXPORT_MODE: 'client',

    /* ------------------------------------------------------------------
     * [4] 화면 파일 경로
     *   상단 내비게이션과 화면 간 이동 링크가 이 값을 씁니다.
     * ---------------------------------------------------------------- */
    PAGES: {
      dashboard: 'index.html',
      simulation: 'simulation.html'
    },

    /* ------------------------------------------------------------------
     * [5] 화면 표기용 메타
     * ---------------------------------------------------------------- */
    APP: {
      name: '화성시 버스 수요·공급 미스매칭 대시보드',
      shortName: 'HWASEONG BUS GAP',
      org: '화성시',
      dept: '교통정책과'
    },

    /* ------------------------------------------------------------------
     * [6] 격자 정의 (참고용 문서)
     *   ⚠️ 실값의 단일 출처는 백엔드 GET /meta 의 meta.grid 입니다.
     *   지도·범례·부제가 전부 meta.grid.sizeMeters/cellCount 를 쓰므로
     *   격자를 세분화(예: 500m)해도 프론트 코드는 고칠 것이 없습니다.
     *   아래 값은 런타임에 참조되지 않는 기록용입니다.
     * ---------------------------------------------------------------- */
    GRID: {
      analysisSizeMeters: 1000,   // 1km 배포판 기준 기록 — 실값은 /meta
      analysisCellCount: 786
    },

    /* ------------------------------------------------------------------
     * [7] 카카오맵 배경 (선택)
     *   지도 카드의 SVG 뒤에 실제 카카오맵을 깔아, 확대하면 건물·지명이
     *   보이게 합니다. 키가 비어 있거나 SDK 로드에 실패하면 자동으로
     *   지금의 SVG 단독 지도로 돌아갑니다(map.js 참고).
     *
     *   ⚠️ 로컬 테스트 전용 키입니다. 카카오는 계정당 첫 번째로 활성화한
     *      앱에만 무료 쿼터를 주기 때문에, 이미 무료 쿼터를 받은 다른 개인
     *      프로젝트("오고 있어?")의 키를 빌려 쓰고 있습니다 — 그 앱의 콘솔에
     *      이 도메인(localhost:8000)을 허용 도메인으로 등록해 뒀습니다.
     *      프론트를 공개 저장소에 커밋하면 이 키도 같이 공개되니, 그 전에
     *      화성 프로젝트 전용 앱을 새로 만들어 결제수단을 연결하거나
     *      (지도 SDK 는 0.1원/건으로 저렴합니다), 아래 jsKey 를 비워
     *      지금의 SVG 전용 지도로 되돌리세요.
     * ---------------------------------------------------------------- */
    KAKAO: {
      enabled: true,
      jsKey: '70b0a9642cb9a174f2946debfde84cb0'
    },

    /* ------------------------------------------------------------------
     * [8] 시뮬레이션 단가
     *   confirmed:false 인 항목이 하나라도 있으면 화면과 보고서에 "가정값"
     *   표시가 자동으로 붙습니다(costIsAssumed).
     *
     *   basis 가 비용의 성격을 나타냅니다. 이게 다르면 그냥 비교하면 안 됩니다.
     *     'capital'   1회성 자본비. lifeYears 로 나눠야 연간 비용이 됩니다.
     *     'operating' 연간 운영비. 매년 발생하므로 나누면 안 됩니다.
     *
     *   2026-08-14 공개자료 대조 — 세 값 모두 처음에는 시연용 가정값이었는데,
     *   공식 자료를 찾아 맞춰 본 결과 둘은 실측과 10% 안쪽으로 들어와
     *   confirmed:true 로 올렸습니다. 근거는 각 항목의 source 에 적었습니다.
     *   나머지 하나(정류장)는 근거를 절반만 찾아 false 로 남깁니다 —
     *   "못 찾았다"를 "확정했다"로 바꾸지 않습니다.
     * ---------------------------------------------------------------- */
    COST: {
      stop: {
        krw: 42000000, basis: 'capital', lifeYears: 10,
        annualMaintenanceKrw: 1000000,
        label: '정류장 신설 1개소',
        /* 구성요소 중 정보안내기(BIT)만 공개 단가를 찾았습니다 —
           독립형 1,600만 원 · 알뜰형(표지판 일체형) 500만 원(서울시 2016).
           승차대 구조물 단가는 지자체 설계내역서 안에만 있어 공개자료에
           없습니다. 참고로 부대공사비를 뺀 스마트쉼터가 성동구 기준
           소형 6,500만 원 · 중형 1억 원(2024)이라, 이 값(4,200만 원)은
           BIT 1,600만 + 승차대로 보면 그 아래에 놓이는 범위입니다. */
        note: '승차대·정보안내기 설치비. 유지관리비는 별도 연 100만 원 가정.',
        source: '일부 확인 — BIT 1,600만 원(서울시 2016)만 공개단가, ' +
                '승차대 구조물은 미확인',
        confirmed: false
      },
      drt: {
        krw: 180000000, basis: 'operating', lifeYears: 1,
        label: '똑버스 1대 연간 운영비',
        /* 경기교통공사 표준운송원가(대당 1일) × 365 로 대조:
             경기도 2023년 기준원가 526,301원 → 연 1억 9,210만 원
             수원시 2024년 원가(안) 560,428원 → 연 2억   456만 원
           이 값(1억 8,000만 원)은 둘보다 6~12% 낮아 보수적입니다.
           ※ 표준운송원가에는 차량 감가상각비가 들어 있는데 아래 note 는
             차량 구매비를 뺐다고 적고 있어, 그만큼 우리가 더 좁게 잡은 셈입니다. */
        note: '기사 인건비·연료·정비·관제 포함 가정. 차량 구매비는 미포함.',
        source: '경기교통공사 표준운송원가 대조 — 경기도 \'23년 기준 526,301원/일·대, ' +
                '수원시 \'24년 560,428원/일·대 (수원시정연구원 정책연구 표3-12)',
        confirmed: true
      },
      freq: {
        krw: 95000000, basis: 'operating', lifeYears: 1,
        label: '배차 증편 (노선 1개 · 1일 4회) 연간',
        /* 화성시 실측으로 대조했습니다 — 시 마을버스 345대의 연간 총 운송원가가
           322억 원이므로 대당 연 9,333만 원입니다. 이 값과 1.8% 차이입니다.
           ⚠️ 전제: '1일 4회 증편'을 차량 1대 추가 투입에 준하는 것으로 봅니다.
             기존 차량의 배차 간격만 조이는 경우라면 이 값은 과대 추정입니다.
             첨두시간대 증편은 그 시간에 차량·기사가 따로 붙어야 해서 사실상
             1대에 준한다고 보고 이 기준을 씁니다. */
        note: '운전기사 추가 인건비와 연료비 가정. 차량 1대 추가 투입에 준하는 규모.',
        source: '화성시 실측 대조 — 마을버스 345대 연간 운송원가 322억 원 ' +
                '= 대당 연 9,333만 원 (경기신문 2025, 2024-01 기준)',
        confirmed: true
      },
      defaultBudget: 3000000000,  // 기본 예산 한도 30억 원

      /* 추천 순위를 매길 때 무엇을 '비용'으로 볼 것인가.
       *
       *   'total'  — 총사업비 (기본값)
       *   'annual' — 연환산 비용 (자본비는 lifeYears 로 나눔)
       *
       * 총사업비를 기본으로 두는 이유:
       *   ① 예산 한도(defaultBudget)와 화면 입력칸이 모두 총액입니다.
       *      순위만 연환산으로 매기면 재는 자와 자르는 자가 달라집니다.
       *   ② 공무원이 실제로 묻는 것은 "이번 예산 30억으로 무엇을 살 수 있나"입니다.
       *   ③ 내용연수 가정이 필요 없습니다. 가정이 하나 줄면 반박 지점도 하나 줍니다.
       *
       * 한계 — 총사업비는 1년차 관점입니다. 똑버스·증편은 이듬해에도 같은 돈이
       * 다시 들어가지만 정류장은 한 번뿐입니다. 다년도 사업비로 비교하려면
       * 'annual' 로 바꾸고 COST[].lifeYears 를 실제 값으로 채우세요.
       * 기준별 결과 차이는 docs/API.md §2.1 에 기록해 두었습니다. */
      compareBasis: 'total'
    }
  };

  /* ------------------------------------------------------------------
   * 단가 조회 헬퍼
   *   COST 항목이 객체가 되면서, 금액만 필요한 곳에서 쓰라고 만든 함수입니다.
   * ---------------------------------------------------------------- */
  CONFIG.costKrw = function (type) {
    var c = CONFIG.COST[type];
    return (c && typeof c.krw === 'number') ? c.krw : 0;
  };
  CONFIG.costMeta = function (type) {
    return CONFIG.COST[type] || null;
  };
  /** 단가 중 하나라도 미확정이면 true — 화면에 "가정값" 표시를 붙입니다 */
  CONFIG.costIsAssumed = function () {
    return ['stop', 'drt', 'freq'].some(function (t) {
      var c = CONFIG.COST[t];
      return c && c.confirmed === false;
    });
  };

  /* API 전체 URL 조합 */
  CONFIG.url = function (path) {
    var base = (CONFIG.BASE_URL || '').replace(/\/+$/, '');
    var pre = (CONFIG.API_PREFIX || '').replace(/\/+$/, '');
    var p = path.charAt(0) === '/' ? path : '/' + path;
    return base + pre + p;
  };

  global.HW = global.HW || {};
  global.HW.CONFIG = CONFIG;
})(window);
