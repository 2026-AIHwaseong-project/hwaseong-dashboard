# 화성시 버스 수요·공급 미스매칭 대시보드

교통카드 실현수요와 통신 유동인구 잠재수요를 같은 격자에서 대조해
**대중교통 사각지대**를 찾아내고, **노선 조정 우선순위**와 **정책 시뮬레이션**을
한 화면에서 제공하는 웹 대시보드입니다.

화성시 AI 공모전 출품작 · 프론트엔드

---

## 바로 실행

빌드 도구가 필요 없습니다. 파일을 열기만 하면 됩니다.

```bash
# 방법 1 — 단일 파일 (가장 간편. 파일 하나만 더블클릭)
dist/hwaseong-dashboard.html

# 방법 2 — 원본 그대로 열기
index.html 을 브라우저로 열기

# 방법 3 — 로컬 서버 (백엔드 연동 시 필수)
python3 -m http.server 5500
# → http://localhost:5500/
```

기본값은 **목(mock) 모드**라 백엔드 없이도 전 기능이 동작합니다.

### 단일 파일 버전

CSS·JS를 HTML 안에 전부 넣은 배포용 파일입니다. 인터넷도 서버도 필요 없어
메신저·메일로 보내거나 USB로 옮겨도 그대로 열립니다. 시연·공유용으로 쓰세요.

```bash
node tools/build-standalone.js     # 원본 수정 후 다시 빌드
```

| 파일 | 용도 |
|---|---|
| `dist/hwaseong-dashboard.html` | 분석 대시보드 (약 165KB) |
| `dist/hwaseong-simulation.html` | 정책 시뮬레이션 (약 176KB) |

> 두 파일은 서로를 링크하므로 **같은 폴더에 함께** 두세요.
> 단일 파일 버전은 목 데이터 전용입니다. 실서버 연동은 원본(`index.html` + `assets/`)을 씁니다.

---

## 화면 구성

| 화면 | 파일 | 하는 일 |
|---|---|---|
| **분석 대시보드** | `index.html` | 격자 미스매칭 지도, 노선 조정 우선순위 Top 10, 수요–공급 4분면, 정류장 시간대 프로파일 |
| **정책 시뮬레이션** | `simulation.html` | 정류장 신설·똑버스 배치·배차 증편을 지도에 배치 → 예산 대비 사각지대 해소 효과를 기준선과 비교 |
| **AI 보고서** | 두 화면의 상단 버튼 | 현재 분석·시나리오를 근거로 검토 보고서 초안을 생성해 **한글(.rtf)·엑셀(.xlsx)** 로 저장 |

---

## 파일 구조

```
├── index.html                분석 화면 (사이트 첫 화면)
├── simulation.html           정책 시뮬레이션 화면
├── assets/
│   ├── css/app.css           ⬅ 디자인 담당: 색·여백·컴포넌트 전부 여기
│   ├── data/
│   │   ├── boundary.js       실제 읍면동 경계 (자동 생성, 직접 수정 금지)
│   │   └── hwaseong-dong.geojson   같은 내용의 GeoJSON (참고용)
│   └── js/
│       ├── config.js         ⬅ 백엔드 담당: 여기 두 줄만 고치면 연동 끝
│       ├── api.js            서버 통신 단일 창구 (모든 fetch 가 여기를 지남)
│       ├── mock.js           목 백엔드 — 응답 형태가 곧 API 규격
│       ├── core.js           공용 유틸(포맷·툴팁·테마·좌표투영·SVG 헬퍼)
│       ├── map.js            격자 지도 컴포넌트 (두 화면 공용)
│       ├── report.js         AI 보고서 + .xlsx/.rtf 생성기
│       ├── dashboard.js      분석 화면 로직
│       └── simulation.js     시뮬레이션 화면 로직
├── dist/                     단일 파일 배포본 (자동 생성)
│   ├── hwaseong-dashboard.html
│   └── hwaseong-simulation.html
├── tools/
│   ├── build-standalone.js   단일 파일 빌드 스크립트
│   ├── build-boundary.py     SGIS SHP → boundary.js 생성기
│   └── dev-server.py         캐시 없는 개발 서버
└── docs/
    ├── API.md                ⬅ 백엔드 연동 규격서 (엔드포인트 9개)
    └── AI-REPORT.md          ⬅ Claude API 호출 규격 (보고서 생성)
```

### 의존성

**없습니다.** 외부 라이브러리를 쓰지 않습니다.
차트·지도는 SVG 직접 생성, 엑셀은 ZIP 컨테이너를 직접 작성합니다.
`npm install` 이 필요 없고, 실행에 빌드도 필요 없습니다.
(`tools/build-standalone.js` 는 배포용 단일 파일을 만들 때만 쓰는 선택 사항이며,
Node 만 있으면 되고 설치할 패키지가 없습니다.)

---

## 팀 역할별 안내

### 🎨 프론트엔드 · 디자인 담당

**`assets/css/app.css` 만 보면 됩니다.**

- 파일 맨 위 `:root` 에 모든 색·그림자가 CSS 변수로 정의돼 있습니다.
  변수 값만 바꾸면 두 화면 전체 톤이 한 번에 바뀝니다.
- 라이트/다크 각각 별도로 정의돼 있습니다(자동 반전이 아니라 각각 조정한 값).
  상단 [테마] 버튼으로 `자동 → 라이트 → 다크` 순환 확인 가능.
- 컴포넌트는 12개 구획으로 나뉘어 있고 목차가 파일 상단에 있습니다.

시각화 색 규칙(지키면 색각 이상·흑백 인쇄에서도 읽힙니다):

| 용도 | 규칙 | 변수 |
|---|---|---|
| 미스매칭 MI | 발산형 — 파랑 ← **무채색** → 빨강 | `--mi0` ~ `--mi6` |
| 수요·유동인구 | 순차형 단일 색상(파랑) | `--sb0` ~ `--sb4` |
| 공급 | 순차형 단일 색상(주황) | `--so0` ~ `--so4` |
| 승차/하차 | 범주형 2색 — 파랑·주황 | `--c-board` / `--c-alight` |
| 전/후 비교 | **색이 아니라 스타일로 구분** (기준선=점선 고스트, 시나리오=채움) | `--ink3` / `--c-board` |
| 좋음/나쁨 | 반드시 부호·라벨과 함께 | `--good` / `--bad` |

#### 나중에 덧붙인 컴포넌트 (톤 조정 부탁드립니다)

`app.css` 뒤쪽에 구획 13~16으로 따로 모아 뒀습니다.
기존 규칙은 건드리지 않았으니 아래 클래스만 보시면 됩니다.

| 구획 | 클래스 | 무엇 |
|---|---|---|
| 13 | `.brief` `.brief-main` `.brief-act` `.brief-tag` | 화면 상단 한 줄 브리핑 |
| 14 | `.lg-syms` `.lg-sym` `.lg-clear` | 지도 기호 범례 (정류장·노선·배치기호 등) |
| 15 | `.rgtbl` `.rg-bar` `.rg-name` `.rg-zero` | 권역별 요약표 |
| 16 | `.help` | 지표 옆 물음표 버튼 |

새 색은 만들지 않았고 기존 CSS 변수만 썼습니다.
**카드 순서와 그리드 배치는 손대지 않았습니다** — 위계 조정은 디자인 담당이 판단하실 부분이라
권역 요약 카드도 일단 맨 아래에 두었습니다. 위로 올리고 싶으시면 `index.html` 에서
`<section class="card span12">` 블록 위치만 옮기면 됩니다.

### 🔌 백엔드 담당

**`docs/API.md` 한 장이면 됩니다.**

1. 엔드포인트 9개 구현
2. `assets/js/config.js` 에서 `BASE_URL` 설정, `USE_MOCK: false`
3. 끝

응답 형태가 헷갈리면 브라우저 콘솔에서 목 응답을 직접 확인하세요.

```js
HW.mock.handle('grid.list', { period: 'am' })
HW.mock.handle('simulations.run', null, { placements: [{type:'stop', cellId:'G-453', count:1}] })
```

**하나만 먼저 붙여 보기** — 격자 API만 실서버로:

```js
ENDPOINT_OVERRIDES: { 'grid.list': false }
```

AI 보고서 생성(Claude API)은 **반드시 백엔드에서** 호출하세요.
프론트엔드에 API 키를 두면 브라우저에서 그대로 노출됩니다.
호출 규격은 `docs/AI-REPORT.md` 에 실행 가능한 코드로 정리돼 있습니다.

---

## 산출 로직

```
D  수요지수   = 0.5·정규화(교통카드 실현 승하차) + 0.5·정규화(통신 유동인구)
S  공급지수   = 0.78·정규화(운행빈도) + 0.22·정류장 커버리지 + 배치효과
MI 미스매칭   = z(D) − z(S)   ← 양수가 클수록 공급 부족
우선순위      = MI⁺ × 수요규모 × (1 + 1.6·고령인구비)
추천 배치안   = 연간 환산 사업비 1원당 미해결 통행 감소량이 큰 순 (탐욕적 선택)
```

**추천은 언어모델이 아니라 최적화 알고리즘이 합니다.** 위치 선정을 LLM 에 맡기면
매번 답이 달라지고 근거를 댈 수 없습니다. AI 는 선정 근거를 문장으로 다듬는 일만 합니다.

설계상 짚어 둘 점 두 가지:

- **실현수요는 공급에 눌립니다.** 버스가 없어서 발생하지 못한 통행이 있으므로,
  교통카드 승하차만 보면 사각지대를 과소평가하게 됩니다. 그래서 통신 유동인구
  기반 잠재수요를 절반의 가중치로 함께 봅니다.
- **정규화 기준은 배치 없는 상태에서 한 번만 잡고 고정합니다.** 매번 다시 잡으면
  시뮬레이션 전후 KPI를 서로 다른 자로 재는 셈이 되어 비교가 무의미해집니다.

---

## 연동 예정 데이터

| 데이터 | 출처 | 쓰임 |
|---|---|---|
| 교통카드 승하차 | 교통카드빅데이터 통합정보시스템(STCIS) | 실현수요, 정류장 프로파일 |
| 유동인구 | 통신사 유동인구 | 잠재수요 |
| 노선·정류장 | 경기버스정보(GBIS) | 공급, 커버리지 |
| 격자 인구 | 통계지리정보서비스(SGIS) 250m 격자 | 고령인구비, 인구 가중 |
| **읍면동 경계** | **SGIS 읍면동 경계 (적용 완료)** | **지도, 권역별 집계** |

---

## 현재 상태 · 남은 일

**동작하는 것**

- 두 화면 전부, 목 데이터 기반으로 완전 동작
- AI 보고서 생성 → 한글(.rtf) / 엑셀(.xlsx) 저장 (외부 라이브러리 없이)
- 시나리오 저장·불러오기(브라우저 로컬 저장)
- 라이트/다크 테마, 반응형(태블릿까지. 폰은 차트 글자가 작아집니다)
- 단일 파일 배포본 (`dist/`) — 서버 없이 더블클릭으로 실행
- 권역(읍면동)별 요약표 — 클릭하면 지도에서 해당 권역만 강조
- 지표 용어 도움말 — 물음표 버튼에 설명 10종
- 지도 기호 범례 — 색 범례와 별도로 정류장·노선·배치기호 설명
- **추천 배치안** — 예산 범위에서 효과가 가장 큰 지점을 알고리즘이 선정,
  선정 근거와 함께 배치 목록에 반영되고 보고서에도 실림

**실데이터 적용 시 반드시 보정해야 하는 값**

| 항목 | 위치 | 비고 |
|---|---|---|
| 배치 효과 계수 | 서버 시뮬레이션 모델 (`docs/API.md` §3.6) | 유사 사례 실측 승하차 증가율로 보정 |
| 수단별 단가 | `assets/js/config.js` 의 `COST` | 실제 사업비 기준 |
| 잠재수요 → 통행량 환산계수 | 서버 (`tripCoef`) | 실측 승하차 총량에 맞춰 보정 |
| ~~지도 경계~~ | ~~`GET /meta`~~ | ✅ **완료** — SGIS 실제 읍면동 경계 적용 |

**남은 일**

- [ ] 백엔드 API 연동 (`docs/API.md`)
- [ ] Claude API 보고서 생성 서버 구현 (`docs/AI-REPORT.md`)
- [ ] 한글 `.hwpx` 원본 포맷 내보내기(선택 — 현재는 한컴오피스가 여는 `.rtf`)

---

## 행정경계 데이터

지도의 읍면동 경계는 SGIS 원본 SHP 에서 만들어 씁니다.
`assets/data/boundary.js` 는 자동 생성 파일이므로 직접 고치지 마세요.

```bash
# SGIS > 자료제공 > 경계 > 읍면동 에서 받은 SHP 세트를 프로젝트 루트에 두고
pip install pyshp pyproj
python3 tools/build-boundary.py
```

원본 SHP(135MB)는 `.gitignore` 처리했습니다. 좌표점을 63,204개에서 5,759개로
줄여(약 25m 허용오차) 122KB 로 만들었고, EPSG:5179 → EPSG:4326 변환을 거칩니다.

---

## 개발용 서버

`python3 -m http.server` 는 캐시 헤더를 보내지 않아, 코드를 고쳐도 브라우저가
예전 `.js` 를 계속 쓰는 일이 생깁니다. 대신 이걸 쓰세요.

```bash
python3 tools/dev-server.py 5500
```

응답마다 `Cache-Control: no-store` 를 붙여 항상 최신 파일을 받아갑니다.

---

## 면책

**지도의 행정경계는 실제입니다** — SGIS 통계지리정보서비스의 화성시 읍면동
경계(4읍 9면 16동)를 그대로 씁니다. 격자도 그 경계 안에만 생성됩니다.

그러나 **수요·공급·미스매칭 지수 등 모든 수치와 노선·정류장은 가상**입니다.
지도가 실제라서 수치도 실제라고 오해하기 쉬우니 주의하십시오.
정책 판단의 근거로 사용할 수 없습니다. 실데이터 전환 후에는 `config.js` 의 `APP.isMockData` 를
`false` 로 바꾸면 화면의 목업 배지와 면책 문구가 사라집니다.
